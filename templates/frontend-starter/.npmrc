# Enforce the Node.js engine range declared in package.json (>=26 <27).
# npm reads the .npmrc of the project it is installing, not ancestor
# directories, so this travels with the starter when it is copied into an
# isolated project directory under WEBSITE_WORKSPACE_ROOT.
engine-strict=true
